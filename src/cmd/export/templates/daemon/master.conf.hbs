pre-start script
{{#with master}}
bash << "EOF"
mkdir -p {{ log_dir_path }}
chown -R {{ user }} {{ log_dir_path }}
mkdir -p {{ run_dir_path }}
chown -R {{ user }} {{ run_dir_path }}
EOF
{{/with}}
end script

start on runlevel [2345]

stop on runlevel [016]
